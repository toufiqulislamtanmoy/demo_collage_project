import { ProvidersSession } from "@/providers/ProvidersSession";
import QueryClientProviderClient from "@/providers/QueryClientProviderClient";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Collage Frontend",
  description: "Generated by Automatic Collage",
};
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <QueryClientProviderClient>
          <ProvidersSession>{children}</ProvidersSession>
        </QueryClientProviderClient>
      </body>
    </html>
  );
}
